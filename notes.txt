git init // for initilizing repository
git status // show status
git add // add file to staging
git commit -m"<file>" // commit change and add a notice with -m"<messege>"
git commit . // commits the whole folder/all files of the folder (notes.txt + index.html)